---
name: workbook-validator-inbound
description: >
  외국계법인(Inbound) TP 분석 워크북(Tap1~Tap17 체계, Tap12-1·13·14·18 부재)의 수리적·논리적
  정합성을 스크립트로 전수 검증하는 게이트 스킬(Phase 0~4). 정상가격 산출결과(Tap17)의
  사분위·다중연도 평균·운전자본 조정 재계산, 시트 간 코드·PLI 표기·수치 연결(Tap15↔17,
  Tap3↔12↔16, Tap12↔16↔17) 교차 대조, 정상범위 판정의 독립 재현, 정상가격 산출 원본과
  Tap17의 전기(轉記) 대조를 수행한다. 다음 상황에서 반드시 사용할 것: 사용자가
  "workbook-validator-inbound 실행", "Inbound 워크북 검증", "외국계법인 워크북 검증",
  "Inbound 정합성 검증", "Inbound Tap17 검토", "Inbound 사분위 검증", "Inbound 산출결과 검토",
  "Inbound 수치 재계산" 등을 언급하는 경우, 또는 Inbound TP 워크북 완성 직후·보고서 착수
  직전의 최종 점검을 요청하는 경우. tp-report-writer-inbound Step 0 실행 전 사전 게이트로
  사용되며, 합격 시 보고서 작성의 데이터 기준선(어느 시트가 정본인지)이 확정된다.
  ※ 내국법인(Outbound, Tap1~18 체계) 워크북은 workbook-validator를 사용한다.
---

# Inbound 워크북 정합성 검증 게이트 (Workbook Validator — Inbound)

## Phase 0 — 무결성 확인 (본론 착수 전 1회)

본 스킬은 검증 규율을 누적해 온 파일이며, 다른 TP 스킬과 서로를 지목하며 맞물려 동작한다.
경고문이 지워지거나 구버전이 섞이면 오류를 검출하는 대신 **주입**하게 되므로, 본론에 들어가기
전에 정본 여부를 먼저 확인한다.

**실행 방법 — 스킬 폴더를 탐색해서 이동한 뒤 실행한다.**

```bash
D=$(find ~ / -maxdepth 8 -type d -name "workbook-validator-inbound" -path "*skills*" 2>/dev/null | head -1)
cd "$D" && python3 scripts/verify_skills.py
```

⚠️ **스킬이 안내하는 base directory 경로를 그대로 쓰지 말 것.** 그 경로는 Windows 형식
(`C:\Users\...`)인데 명령을 실행하는 셸은 **Linux 경로 체계**를 쓴다. 같은 폴더인데 이름이
달라 `No such file or directory`가 난다(실행 검증에서 실제로 확인한 오류다).
위 탐색 방식은 **경로 체계와 무관하게 동작**하며 약 1초 걸린다.

폴더 안에서 실행하면 **인자가 필요 없다.** 스크립트가 루트와 대상 스킬을 스스로 판별한다.

- **합격** — 그대로 진행한다. 별도로 보고하지 않는다.
- **변조·누락** — **작업을 중단하고 사용자에게 고지**한다. 어느 파일이 정본과 다른지 알린다.
  사용자가 진행을 지시하면 「무결성 미확인 상태」임을 답변에 명시하고 진행한다.
- **실행 불가**(경로·스크립트·매니페스트 없음) — 검증을 **생략하되 침묵하지 않는다.**
  한 줄로 「무결성 확인 미실행」을 알리고 진행한다.
- **`[미설치] tp-case-law`가 보이면** — 본 스킬이 조문·판례 검증을 tp-case-law에 맡기는
  부분은 실행되지 않는다. 해당 단계에서 그 사실을 고지하고, 조문 인용은 「확인 필요」로 표기한다.

⚠️ **이 절차는 변조를 막지 못한다. 드러나게 할 뿐이다.** 매니페스트(`scripts/MANIFEST.sha256`)가
함께 바뀌면 검증은 통과한다. 배포 시에는 매니페스트를 변경 이력이 남는 곳에도 보관하고 대조한다.

---


## 0. 기본 원칙

- **전제**: 워크북은 여러 도구(DB 추출, Claude for Excel 프롬프트, 수작업)가 채운 시트의
  집합이므로 코드 체계·표기·수치가 어긋나 있을 수 있다. Claude는 스크립트(openpyxl 등)로
  **전수 재계산·교차 대조**하고, 발견사항의 처리는 **항상 사용자가 확정**한다.
- **실행 시점**: Inbound 워크북 완성 직후 · tp-report-writer-inbound Step 0 착수 직전.
  보고서는 워크북 수치를 그대로 전기하므로, 오류는 반드시 이 게이트에서 워크북 단계에 수정한다.
- **게이트 원칙**: 각 Phase는 「검증 → 발견사항 보고(심각도 분류) → 사용자 확정」을 거치고
  `progress.md`에 기록한다. 구동 지시어는 "Phase N 실행", 순서는 0 → 1 → 2 → 3 → 4.
- **검증이지 재분석이 아니다**: 비교가능회사 선정의 타당성(comparable-search-review-inbound
  소관), 특성분류·Tested Party 논거(characterization-gate-inbound 소관), 방법 선택 논거
  (Tap16 프롬프트 소관)는 범위 밖. 본 스킬은 수리·표기·전기(轉記) 정합성만 다룬다.

### Inbound 워크북 체계 (본 스킬의 검증 기준)

| 항목 | 기준 |
|---|---|
| 표준 시트 체계 | Tap1~Tap17 (**Tap12-1·13·14·18 부재가 정상** — 누락으로 보고하지 않음) |
| 특성결정·방법 확정 | Tap12(이전가격특성결정) + Tap16(가장합리적인방법선택) |
| 평가단위 | 통상 **한국법인 단일 Tested Party** (검색 트랙은 복수 가능 — 예: 상품중계·서비스 제공업) |
| 산출결과 정본 | **Tap17이 유일 정본** (Tap18 없음) |
| 선행 게이트 | characterization-gate-inbound |
| 후속 스킬 | tp-report-writer-inbound |
| 전형적 PLI | Berry Ratio(GP/OE), OM/TC 등 |

⚠️ Header의 Engagement Type 표기가 파일명·내용과 반대로 기재된 실전 사례가 있다.
표기를 그대로 믿지 말고 **실제 거래 방향으로 사용자 확정**한다. Outbound 워크북(Tap12-1·14·18
존재)으로 판명되면 본 스킬을 중단하고 **workbook-validator로 전환**한다.

- **시트명 정규화**: 시트명은 공백·언더스코어가 불규칙하다(` Tap1-2`, `Tap 9_FAR분석 종합`,
  `Tap7_회사조직도 ` 등). 공백·언더스코어를 제거한 정규화 문자열로 `TapN` 접두를 매칭한다.

### 심각도 분류 (모든 발견사항에 부여)

| 등급 | 정의 | 처리 |
|---|---|---|
| **A (결론 영향)** | 정상범위·판정·평가단위가 달라질 수 있는 오류 (사분위 오계산, 시트 간 범위 상이, 논리 오류, **원본의 다른 PLI 행을 전기한 오류**, **최종 표본의 원문 확인 기록 부재**) | 최우선 확정 — 미해결 시 게이트 불합격 |
| **B (표기·라벨)** | 결론은 같으나 보고서 전기 시 혼선 유발 (PLI 명칭 상충, 코드 이중 사용, 거래방향 반대 기재, 법인 약칭 오기) | 정본 표기 확정 후 통과 가능 |
| **C (데이터 품질)** | 이상 징후 (연도 간·회사 간 동일값 복제, 결측, 단위 혼재) | 사유 확인·기록 후 통과 가능 |

---

## 1. Phase 0 — 인수 및 기준선 설정

1. **워크북 원본 xlsx** — 수식 보존 상태. 값 붙여넣기 시트는 원본 산출 파일을 함께 요청.
   **Engagement Type 확정** — Inbound 확인 (Outbound 판명 시 workbook-validator로 전환).
2. **선행 게이트 검토조서 인수** — characterization-gate-inbound 검토조서(코드 대응표·
   Tested Party 요약표)와 comparable-search-review-inbound 방어 패키지가 있으면 인수하여
   **해당 항목은 재검증하지 않고 인용**한다. 없으면 Phase 1에서 코드 검증을 직접 수행한다.
3. **평가단위·트랙 목록 확정** — 통상 「한국법인 단일 Tested Party × 단일 PLI」이나, 검색
   트랙이 복수(예: 상품중계업·서비스 제공업)이면 트랙 목록도 함께 확정한다
   (트랙은 평가단위가 아니라 동일 평가단위의 표본 구성 단위임을 구분). 이 표가 Phase 2~3의
   검증 단위가 된다.
4. **정상가격 산출 원본** (필수) — Tap17이 전기해 온 산출 파일(예: `정상가격분석_*.xlsx`).
   ⚠️ **워크북 내부 검증만으로는 전기 오류를 검출할 수 없다.** Tap17은 사분위·조정 구조가
   내부적으로 완전히 일관하면서도 원본의 **다른 PLI 행**을 옮겨온 상태일 수 있다
   (실전 검출례: 원본이 8종 PLI를 병렬 산출하는데 채택 행이 아닌 첫 행 Gross margin이
   전기되어 정상범위가 3배로 뒤바뀐 사례). 미제출 시 **Phase 2-0을 수행할 수 없으므로
   그 사실과 재현 한계를 검증 보고서에 명시**하고 조건부로만 진행한다.
   원본이 복수 시나리오 시트를 포함하면 **정본 시나리오를 사용자에게 확정**받는다.

5. **비교가능회사 검색 과정표** (필수) — comparable-search-review-inbound 방어 패키지 1
   (최종 검색과정 표, 통상 `부록 2-1 *.xlsx`). **Phase 1 항목 8의 검사 대상**이다.
   ⚠️ 워크북 시트에는 재판정 근거가 없다. 과정표를 인수하지 않으면 최종 표본의 원문 확인
   이행 여부를 확인할 수단이 없다. 미제출 시 **[등급 A]로 기록하고 조건부로만 진행**한다 —
   「과정표 미인수」는 「확인 기록 없음」과 같은 위험이며, 조용히 건너뛰지 않는다.
   트랙이 복수이면 트랙별 과정표를 모두 인수한다.

게이트: 워크북 인수 + Engagement Type(Inbound) 확정 + 평가단위·트랙 기준표 확정 +
정상가격 산출 원본 인수(또는 미제출 사유 기록) + **검색 과정표 인수(또는 미제출 A등급 기록)**.

---

## 2. Phase 1 — 구조·코드·표기 스캔

전 시트를 스캔하여 사전(dictionary)을 구축하고 상충을 검출한다.

1. **시트 완비성** — Inbound 표준(Tap1~17) 대비 존재·누락·추가 시트 목록.
   Tap12-1·13·14·18 부재는 정상이므로 누락으로 분류하지 않는다.
2. **코드 사전** — 법인코드, 거래코드(TR00n/TRE_00n)의 전 시트 출현 위치 수집:
   - 거래코드 이중 체계의 대응표 확정 (Tap3 vs Tap12·16 서술)
   - 동일 금액 거래의 상대방만 다른 이중 기재 검출 (이중계상 여부 확인) [등급 B]
   - **법인 약칭 오기 검출** — 시트별 약칭 표기 차이(실전 검출례: AFRKT/AFRTK)는 정본 약칭
     확정 [등급 B]
3. **PLI 표기 사전** — **Tap12·15·16·17**에서 PLI 명칭·산식을 수집하고 상충 검출.
   ⚠️ 실전 검출례(Inbound): Tap15 검색전략 "OM/TC(Cost Plus)" vs Tap16/17 "Berry Ratio
   (GP/OE)" 혼재 — 원칙적 정본은 Tap16/17 [등급 B, 산식 상이 시 A].
   ⚠️ **라벨 상충은 표기 문제로 단정하지 말 것** — 실전에서 라벨 상충의 실제 원인이 **원본에서
   다른 PLI 행을 전기한 오류**였던 사례가 있다. 본 Phase에서는 상충 사실만 기록하고,
   **실질 판정은 Phase 2-0의 원본 대조로 확정**한다.
4. **거래방향** — 서비스수수료·사용료 등 방향성 거래의 수취↔지급 방향이 시트 간 반대로
   기재되지 않았는지 (수수료 수취 주체가 한국법인인지 전 시트 일관 확인).
5. **단위·통화** — 천원/백만원/USD 등 단위 표기의 시트 간 일관성, %와 소수·배수(Berry Ratio)
   혼용 검출. 계약 Markup율과 실현치의 구분 표기 확인
   (실전 검출례: 계약 Cost Plus 10%인데 특정 연도 실현치 18.6%가 계약률처럼 기재).
6. **법령 인용 검증** — 전 시트에서 `국조법|국조령|국조칙|OECD TPG` 인용을 정규식으로
   수집하고 **국가법령정보센터 현행 조문과 대조**한다. 워크북 내부 일관성만으로는 검출되지 않으므로
   외부 대조가 필수다.
   - ⓐ **조번호 실재 여부** — 특히 「~의2」 가지조문은 실재하지 않는 경우가 있다 [등급 A]
   - ⓑ **조문 제목과 인용 취지의 일치** — 조번호는 실재하나 다른 내용을 규정하는 경우 [등급 A]
   - ⓒ **개정 전후 번호 혼재** — 국조법은 2021년 전부개정(법률 제17651호)으로 전면 재번호되었다.
     구 번호(제5조 산출방법·제11조 문서화 등)와 현행(제8조·제16조)이 섞이면 [등급 A]
   - ⓓ **인용 표기 통일** — `OECD TP §3` vs `OECD TPG 제III장` 등 [등급 B]
   - ⓔ **과세연도 적용 판본** — 조문에 개정 표시가 있으면 해당 과세연도에 적용되는 판본을 명시
   ⚠️ 실전 검출례(Case 002): 「국조령 §15의2(정상가격 범위)」는 **존재하지 않는 조문**, 「국조령 §6
   (거래 통합)」의 제6조는 실제로 **재판매가격방법**, 문서화 점검표 전체가 **2021년 전부개정 전
   구 번호**. 총 12건이 3개 게이트를 통과한 후 보고서 집필 단계에서 검출되었다.
7. **마스터 시트 우선순위** — 같은 사실이 복수 시트에 있으면 마스터를 확정하고 상충을 검출한다
   (지분율=Tap1-1, 거래 배정=Tap16, 산업 조정점수=industry-analysis-review-inbound 확정표,
   최종 표본=**Tap17 최종 목록**).
   ⚠️ 실전 검출례: Tap1-2 관계도 「100% 자회사」 vs 마스터 Tap1-1 「82.63%·50.96%」 [등급 B].

8. **원문 확인 기록 완비성 검사** — Tap17 최종 목록(항목 7의 마스터)에 오른 **전건**에 대해,
   검색 과정표의 「재판정 근거」 열에 `rcpNo · 주석 번호 · 재판정일 · 확인 주체 · 확인 수단`이
   **기재되어 있는지만** 검사한다.

   ⚠️ **본 항목은 판단이 아니라 검사다.** 확인 내용이 옳은지, 그 회사가 비교가능한지는
   comparable-search-review-inbound 소관이며 본 스킬은 다시 판단하지 않는다(§7). 있는지 없는지만
   본다 — 그래서 스크립트로 전수 검사할 수 있고, 그래서 이 게이트가 마지막 차단선이 된다.

   - ⓐ 필드 누락(열 자체 부재 포함) → **[등급 A]**
   - ⓑ 확인 주체 = `미확인` → **[등급 A]**
   - ⓒ 확인 주체 = `실무자기재`이고 해당 회사가 **사분위 형성사**(Q1·중앙값·Q3 형성) 또는
     **단독 배제 시 판정 반전사** → **[등급 A]**
   - ⓓ 확인 주체 = `실무자기재`이고 그 밖의 최종 표본 → **[등급 B]**
   - ⓔ 확인 주체 = `사용자제공`인데 출처 자료원·조회일 기재 없음 → **[등급 B]**
   - ⓕ 어휘 외 자유 서술(예: 「확인함」·「DART 참조」) → **[등급 B]**, 정본 어휘로 확정 요구

   **사분위 형성사·판정 반전사의 판별은 Phase 2 재계산 결과를 쓴다.** Phase 1 시점에 미확정이면
   최종 표본 **전건을 ⓒ로 간주**하고, Phase 2 종료 후 등급을 확정한다(과다 검출 후 축소이지
   그 반대가 아니다).

   ⚠️ **실전 검출례**: 2026-08-11 Inbound 사안에서 최종 4사 전건의 확인 주체가 실질적으로
   `실무자기재`였고(당시 열 자체가 없었다), 그중 1사는 **단독 배제 시 하한이 1.0867 → 1.1626으로
   올라 분석대상법인의 실현치 1.10이 정상범위를 이탈**하는 회사였다. 세 게이트가 이를 통과시켰다.

게이트: 코드·PLI·방향·단위·법령 인용 사전 완성, 마스터 시트 확정, 상충 전건 정본 확정,
**최종 표본 전건의 원문 확인 기록 완비성 검사 통과(A등급 0건)**.

---

## 3. Phase 2 — Tap17 수치 전수 재계산

평가단위(·트랙)별로 정상가격 산출표를 독립 재현한다. Inbound는 조정 전/후 Berry Ratio 표가
전형적 대상이다.

**0. 원본 전기 대조 (선행 필수)** — Phase 0에서 인수한 정상가격 산출 원본과 Tap17을 대조한다.
   - **채택 PLI 행의 특정** — 원본이 복수 PLI를 병렬 산출하는 경우(Gross margin, Cost-plus
     Markup, Berry Ratio, Operating margin, Cost Cover Ratio 등), 보고서가 채택한 행을
     **라벨과 산식(예: Berry = GP/OE)까지 명시적으로 특정**한다.
   - **전수 대조** — 비교가능회사별 PLI 값·사분위(Q1/Med/Q3)·분석대상 실현치가 **채택 행에서
     전기되었는지 회사 단위로 전건 대조**한다. 다른 행에서 왔으면 [등급 A].
   - **분석대상 ↔ 비교대상 PLI 정합** — 양측이 동일 행(동일 산식)에서 산출되었는지 확인한다.
     의도적으로 다른 지표를 적용한 경우 근거가 문서화되어 있는지 확인하고 미문서화 시 [등급 B].
   - **시나리오 혼용 검출** — 원본에 복수 시나리오 시트가 있을 때, 정상범위와 실현치가
     **서로 다른 시나리오에서 인용되지 않았는지** 확인한다 [등급 A].
   - **역산 기법** — 값이 재현되지 않으면 원본의 각 후보 행(및 기간 조합: 3개년/5개년,
     연도별/합계)을 순회 대조하여 **실제 산출 기준을 역으로 식별**하고 보고한다.

1. **사분위 재계산** — 조정전·조정후·연도별·평균 각각에 대해 Q1/Median/Q3를 재계산.
   보간 방식(QUARTILE.INC / EXC)을 식별해 명기하고, 정상가격 범위 관점의 방식 선택 근거를
   기록한다(조문 확인은 tp-case-law). 원본 값과 불일치 시 [등급 A].
2. **다중연도 평균 재계산** — 개별 회사의 연도별 값 → 기간 평균 재현 (단순평균 vs 가중평균
   식별). **평균 산출에 포함된 연도 수와 헤더의 연도 범위 일치 확인.**
   ⚠️ 실전 검출례(Inbound): 헤더는 「Ave (23-25)」인데 연도 열이 2023·2024 2개뿐 —
   평균의 실제 모수 확인 필요 [등급 A 또는 B].
3. **조정계산 재현** — 운전자본 조정(AR/INV/AP) 등 조정전→조정후 변환을 가능한 범위에서
   재현. Inbound 판매지원법인은 AR=0·재고=0이 전형이므로 **분석대상 측 조정치 0의 타당성**도
   재무제표(Tap3-2)로 확인한다. 조정 기준금리의 출처·적용 일관성을 확인한다.
   재현 불가 시(조정 파라미터 미수록) 원본 산출 파일을 요청 [미수록 자체가 C].
4. **Tested Party 실현치** — 실현 Berry Ratio 등이 Tap3-2 재무분석에서 재현되는지,
   특수 처리의 산식이 기록되어 있는지. Cost Plus 계약 사안은 계약률(예: 1.10)과 실현치의
   미세 차이 사유를 확인한다.
5. **범위 판정 재현** — 실현치의 정상범위 내/이탈 판정을 독립 재현 (연도별 + 다중연도 평균).
6. **데이터 품질 스캔** — 연도 간·**회사 간** 값 복제 의심, 결측, 극단 자릿수 오류
   [등급 C — 사유 확인 필수].
   ⚠️ 실전 검출례(Inbound): 서로 다른 두 회사의 동일 연도 Berry Ratio가 소수점 15자리까지
   일치(복사 오류 의심), 특정 회사의 단년도 값이 표본 평균 대비 극단 이탈(0.219 vs 1.0대).

게이트: 평가단위(·트랙) 전체 재계산 완료, A등급 전건 해소.

---

## 4. Phase 3 — 시트 간 교차 정합성 검증

교차 검증 8종 (Inbound 경로):

1. **산출결과 정본 확정 (Tap17 ↔ Tap16 요약)** — Tap18이 없으므로 **Tap17이 유일 정본**이다.
   Tap16 §3~5(방법선택 시트에 요약된 사분위·실현치·결론)와 Tap17의 일치를 대조한다.
   상충 시 비교표 제시 후 사용자 확정 [등급 A]. 확정 결과는 tp-report-writer-inbound
   Step 7(경제적 분석)의 데이터 기준선으로 progress.md에 기록.
2. **Tap15 ↔ Tap17** — 최종 비교가능회사 목록의 회사 수·명칭이 검색 결과(Tap15)와 산출표
   (Tap17)에서 일치하는지 (명칭 축약 차이는 B, 회사 수 상이는 A).
   **복수 트랙 사안은 트랙별 최종 선정 수의 합계가 Tap17 목록과 일치하는지** 대조한다
   (예: 도매 3 + 서비스 4 = Tap17 7개사).
3. **방법·Tested Party·PLI 3자 일치** — **Tap12(§5 특성결론·방법 예고) ↔ Tap16(채택) ↔
   Tap17(산출 적용)**의 일치.
4. **거래 배정 완전성 (Tap3 ↔ Tap12·16)** — 전 국외특수관계거래가 분석 대상 또는 분석 제외
   사유에 배정되었는지 — 배정의 완전성만 확인, 통합 논거는 검증하지 않음.
5. **사용료 일관성** — (사용료 거래가 존재하는 경우에만) 사용료율 범위와 분석 결과·Tap17의
   범위 일치. Inbound 판매지원 사안은 통상 해당 없음 — 해당 없음 사실을 기록하고 통과.
6. **재무 연결** — Tap3-2 재무분석의 합계·연도가 Tested Party 실현치 및 Tap1 개요의 재무
   요약과 모순되지 않는지 (상호 변경 이력이 있으면 연도별 법인명 각주 확인).
7. **동일 시트 내 서술 정합성** — 시트 **간** 대조만으로는 검출되지 않는 유형이다.
   한 시트 안에서 **결론이 서로 모순되는 서술**이 공존하는지 확인한다.
   - 정정 이력이 있는 시트는 **정정의 파급 범위**를 점검한다. 결론행만 갱신하고 근거·사실평가 열이
     정정 전 상태로 남는 사례가 전형적이다
   - 같은 시트에서 「범위 내 / 범위 이탈」, 「조정 불요 / 조정 필요」가 함께 나타나면 [등급 A]
   - 구 수치(정정 전 사분위·실현치)가 서술 텍스트에 잔존하는지 정규식으로 스캔
   ⚠️ 실전 검출례(Case 002): Tap16의 사실평가는 「정상범위 하회 → 조정 필요」, 같은 시트 최종
   결론은 「조정 불요」로 **정면 충돌**. PLI 오전기 정정 시 결론행만 갱신된 결과였다 [등급 A].
8. **트랙 완전성** — Tap15 검색 트랙 개수 ↔ Tap17 산출 블록(조정 전/후 × 트랙 통합 표) 대조,
   Tap16이 전제하는 표본 구성과 일치 확인. 어느 하나에 빠진 트랙이 있으면 [등급 A].

게이트: 교차 검증 8종 완료, A등급 전건 해소, 데이터 기준선(정본 시트) 확정.

---

## 5. Phase 4 — 종합 판정·검증 보고서

**검증 보고서 구성 6종** (progress.md에 확정 규칙으로 기록):

1. 합격/보완 체크리스트 — Phase 1~3 발견사항 전건의 등급·처리 결과
2. 재계산 로그 — 사분위·평균·조정·판정의 재현 결과와 사용 산식
3. 코드·PLI·단위 정본 사전 — 보고서 작성 시 표기 기준
4. 데이터 기준선 확정표 — 시트 간 상충 항목별 정본 지정 (Tap17 vs Tap16 요약 등)
5. 미해결 C등급 목록 — 사유·후속 조치
6. **법령 인용 정본표** — 워크북에 등장하는 전 조문의 「인용 위치 · 기재 내용 ·
   현행 조문 확인 결과 · 정본 표기」. 보고서 집필 시 그대로 인용할 수 있도록 확정한다

**합격 조건**: A등급 전건 해소 + B등급 정본 확정. 합격 시 tp-report-writer-inbound Step 0
진행 가능을 고지한다 (Step 0은 본 보고서를 인수하여 자체 검증을 생략·단축할 수 있다).
불합격 시 워크북 수정 → 해당 Phase만 재실행한다.

---

## 6. 게이트 체크리스트

| Phase | 통과 조건 |
|---|---|
| 0 | 워크북(수식 보존)·**정상가격 산출 원본** 인수, Engagement Type(Inbound) 확정, 선행 게이트 조서 인수 여부 기록, 평가단위·트랙 기준표·정본 시나리오 확정 |
| 1 | 코드·PLI(Tap12·15·16·17)·방향·단위·법령 인용 사전 완성, 마스터 시트 확정, 상충 전건 정본 확정, **원문 확인 기록 완비성 검사 통과(A등급 0건)** |
| 2 | 원본 전기 대조(2-0) 전건 일치, 사분위·평균·조정·판정 재현, A등급 전건 해소 |
| 3 | 교차 검증 8종 완료(Tap17↔16 정본 확정·트랙 완전성 포함), 데이터 기준선 확정 |
| 4 | 검증 보고서 6종 고정, 합격 판정, tp-report-writer-inbound Step 0 진행 고지 |

## 7. 중첩 해소 원칙

| 잠재 중첩 | 해소 |
|---|---|
| Phase 1 코드 검증 ↔ characterization-gate-inbound Phase 4 체크리스트 | 선행 게이트 조서가 있으면 인수·인용만, 재검증 금지. 없으면 본 스킬이 수행 |
| Phase 3 Tap15 대조 ↔ comparable-search-review-inbound Phase 2(워터폴) | 검색 과정 내부의 수식·워터폴은 review 소관. 본 스킬은 최종 목록이 Tap17에 올바로 전기되었는지만 |
| Phase 2 실현치 재현 ↔ tp-report-writer-inbound Step 7 | 본 스킬이 검증·확정, Step 7은 확정값을 전기만 |
| PLI 명칭 실질 검증 ↔ characterization-gate-inbound Phase 4 | 선행 게이트는 **표기 불일치의 검출·이관까지만** 수행하고, 실질 판정은 본 스킬 **Phase 2-0(원본 전기 대조)** 에서 확정한다 |
| 원문 확인의 **판단** ↔ **기록 이행 검사** | 특수관계 비중·기능 실체의 판단과 원문 확인의 **수행**은 comparable-search-review-inbound Phase 3B 소관. 본 스킬 Phase 1 항목 8은 **그 이행이 기록으로 남았는지만** 기계 검사하며, 내용의 당부를 재판단하지 않는다. 판단을 두 곳에 두면 둘 다 상대를 믿고 넘긴다 |

## 8. 경계 (중요)

- 오류를 발견해도 Claude가 워크북을 직접 수정하지 않는다 — 수정 주체는 실무자이며,
  본 스킬은 위치(시트·셀)·등급·권고안을 보고한다.
- 정상범위 방식(사분위 보간법, 다중연도 가중치)의 규범적 타당성 판단이 필요하면
  tp-case-law 스킬로 근거(현행 국조령 조문, TPG paras. 3.55~3.66)를 확인해 인용한다.
- 값 붙여넣기로 수식이 소실된 시트는 재현 한계를 보고서에 명시한다 (재현 불가 ≠ 오류).
- **외부 대조는 본 스킬의 범위에 포함된다.** 「검증이지 재분석이 아니다」는 원칙은 비교가능회사
  선정·특성분류 등 **판단**의 재수행을 금지하는 것이지, 법령 조문·산출 원본 등 **사실**의 대조를
  금지하는 것이 아니다. 워크북 내부 정합성만 보는 검증은 「내부적으로 일관된 오류」와
  **「내부적으로 일관된 미검증」**을 함께 통과시킨다. 후자는 전 시트의 수치가 서로 맞는데
  **그 수치의 원천을 아무도 확인하지 않은** 상태를 말하며, 내부 대조로는 원리상 검출되지 않는다
  — 옮겨 적기는 언제나 정합하기 때문이다. **미검증 항목이 정상범위·판정을 좌우하면 통과시키지
  않는다**(Phase 1 항목 8).
- 정상가격 산출 원본이 미제출된 상태의 "합격"은 **조건부 합격**이며, 그 취지(전기 대조 미실시)를
  검증 보고서와 progress.md에 명시한다. 원본 확보 후 Phase 2-0만 재실행한다.
- Tap12-1·13·14·18 부재를 이유로 불합격 처리하지 않는다 — Inbound 체계표가 기준이다.
- Outbound 워크북(Tap1~18 체계)은 본 스킬 범위 밖 — workbook-validator로 전환한다.

---

## 9. 개정 이력

| 버전 | 일자 | 내용 |
|---|---|---|
| v1.0 | 2026-07-22 | 최초 작성 — workbook-validator v1.2(Outbound)를 모체로 Inbound 전용 재구성. Tap1~17 체계 고정, PLI 수집 대상 Tap12·15·16·17, Phase 3 교차검증 Inbound 경로(Tap17↔16 정본, Tap12↔16↔17 3자 일치, 트랙 완전성), Inbound 실전 검출례 반영(약칭 오기, PLI 혼재, 평균 헤더-연도 불일치, 회사 간 동일값 복제, 계약률-실현치 혼기). 모체의 범용 교훈(Phase 2-0 원본 전기 대조, 법령 인용 검증, 동일 시트 내 정합성)은 전부 승계. 근거 사안: TP_2026_003_APRIL_Inbound |
| **자체검증 편입** | **2026-08-11** | **Phase 0 「무결성 확인」 신설** — `scripts/verify_skills.py`와 `scripts/MANIFEST.sha256`을 스킬에 동봉하고, 본론 전에 정본 여부를 확인하도록 했다. 합격 시 침묵, 변조·누락 시 중단·고지, **실행 불가 시에도 침묵하지 않고 미실행을 알린다**. 매니페스트가 함께 바뀌면 통과한다는 한계를 명기. TP 스킬 전체 동결(2026-08-11, 13종)에 편입 |
| **Phase 0 실행 보강** | **2026-08-11** | **실행 검증에서 발견한 결함 정정** — Phase 0의 `python3 scripts/verify_skills.py`가 작업 디렉터리를 스킬 폴더로 가정하고 있어, 지시대로 따라하면 `can't open file` 오류가 났다. **스킬 폴더로 이동하는 방법 두 가지**(location 경로 사용 / 탐색)를 명시 |
| **실행 검증 반영** | **2026-08-11** | **실제 스킬 호출로 발견한 결함 2건 정정.** ① **Phase 0 실행 방법을 탐색 방식으로 일원화** — 스킬이 안내하는 base directory는 Windows 경로(`C:\Users\...`)인데 명령을 실행하는 셸은 Linux 경로를 쓴다. 그대로 `cd` 하면 `No such file or directory`가 난다. 경로 체계와 무관하게 동작하는 탐색 방식만 남겼다. ② 개정 이력 표가 빈 줄로 끊겨 **두 개로 분리**돼 있던 서식 결함 정정 |
| **v1.1** | **2026-08-12** | **원문 확인 이행의 기계적 검사 신설 — 본 스킬을 최종 차단선으로 확정.** ① **§2 Phase 1 항목 8 「원문 확인 기록 완비성 검사」 신설** — Tap17 최종 목록 전건에 대해 과정표 재판정 근거 열의 `rcpNo·주석·재판정일·확인 주체·확인 수단` 기재 여부를 기계 검사. 필드 누락·`미확인`·사분위 형성사의 `실무자기재`는 [등급 A], 그 밖은 [등급 B]. **판단하지 않고 검사만 한다.** ② **§1 Phase 0 인수 항목 5 「비교가능회사 검색 과정표」 신설(필수)** — 재판정 근거는 워크북이 아니라 과정표에 있어, 미인수 시 항목 8이 대상 없이 헛돈다. 미제출은 [등급 A] 조건부 진행. ③ **§8 경계에 「내부적으로 일관된 미검증」 병기** — 옮겨 적기는 언제나 정합하므로 내부 대조로는 원리상 검출되지 않음을 명기. ④ §0 심각도 A 정의에 「최종 표본의 원문 확인 기록 부재」 추가, §6 Phase 1 행·§7 중첩 해소 1행 반영. 계기: 07번 조서(2026-08-12) — review 게이트는 이미 한 번 통과시켰으므로 같은 스킬에 규율을 더 얹는 방식은 같은 실패를 반복한다는 판단. **comparable-search-review-inbound v1.6과 분리 배포하지 않는다** — 확인 주체 열이 없는 상태에서 본 검사만 배포되면 전 사안이 A등급으로 막힌다 |
